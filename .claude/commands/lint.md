Fix all lint errors and warnings in my codebase.

Use parallel subagents to run and fix:

- `npm run build` (builds loveable)
- `npm run lint` (lints entire monorepo)
- `npm run type-check` (checks TypeScript across monorepo)
